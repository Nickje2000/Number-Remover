# Number-Remover
A simple python script to remove the 1. 2. etc. from files.

## Requirements
- Python 3.x

## Usage
Just put the folder in the folder variable (EG 'C:\Music') and then run using python.
